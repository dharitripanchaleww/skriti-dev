<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>{{ env('Name') }}: Log In</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta content="iFour, Your Smart House or Office Solution." name="description" />
        <meta content="Coderthemes" name="author" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <!-- App favicon -->
         <link rel="shortcut icon" href="{{ asset('assets/images/favicon.png') }}">

        <!-- App css -->
        <link rel="stylesheet" href="{{ asset('assets/theme/css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('assets/theme/css/icons.min.css') }}">
        <link rel="stylesheet" href="{{ asset('assets/theme/css/app.min.css') }}">
        <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
        <style>
            body {
                padding-bottom: 0;
            }
            .main-div {
                display: flex;
                height: 100vh;
                align-items: center;
            }
            .section {
                display: flex;
                justify-content: center;
                align-items: center;
                flex-grow: 1;
                flex-shrink: 1;
                flex-basis: 0%;
            }
        </style>
    </head>
    <body>
        <div class="main-div">
            <div class="section">
                <div class="card bg-pattern">
                    <div class="card-body p-4">
                        <div class="text-center w-75 m-auto">
                            <a href="">
                                <span><img src="{{ asset('assets/images/logo.svg') }}" alt="" height="60"></span>
                            </a>
                            <p class="text-muted mb-4 mt-3">{{ title }}</p>
                        </div>
                        <form id="form" action="" method="POST" novalidate>
                            @if(!error)
                                <div class="form-group mb-3">
                                    <label for="password">New Password</label>
                                    <input
                                        id="password"
                                        class="form-control"
                                        type="password"
                                        name="password"
                                        placeholder="Enter your password"
                                        data-parsley-required-message="Please enter your new password."
                                        data-parsley-password="true"
                                        required
                                        >
                                </div>
                                <div class="form-group mb-3">
                                    <label for="conf_password">Confirm New Password</label>
                                    <input
                                        id="conf_password"
                                        class="form-control"
                                        type="password"
                                        name="conf_password"
                                        placeholder="Enter your password"
                                        data-parsley-required-message="Please confirm your new password."
                                        data-parsley-password2="true"
                                        required
                                        >
                                </div>
                                <div class="form-group mb-0 text-center">
                                    <button class="btn btn-default btn-block" type="submit"> Create Password </button>
                                </div>
                            @else
                                <div class="alert alert-danger bg-danger text-white border-0 fade show" role="alert">
                                    {{ error }}
                                </div>
                            @endif
                        </form>
                    </div> <!-- end card-body -->
                </div>
            </div>
            <div class="section">
                <img
                    class="logo-main"
                    src="{{ asset('assets/images/logo.svg') }}"
                    alt=""
                    height="200"
                    />
            </div>
        </div>

        <script src="{{ asset('assets/theme/js/vendor.min.js') }}"></script>
        <script src="{{ asset('assets/theme/js/app.min.js') }}"></script>
        <script src="{{asset('assets/js/parsley.min.js')}}"></script>
        <script src="{{asset('assets/js/create_password.js')}}"></script>
    </body>
</html>